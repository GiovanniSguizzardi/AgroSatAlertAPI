version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: agrosat-mysql
    environment:
      MYSQL_ROOT_PASSWORD: agrosat2026
      MYSQL_DATABASE: agrosat_db
    ports:
      - "3306:3306"
    volumes:
      - agrosat_data:/var/lib/mysql

volumes:
  agrosat_data: